# URL of the Google Sheet listing papers
# The sheet must be shared with your service account or publicly. See 'Setup'
# section of README.md for details.
sheet_url: null

# Email to include in crossref queries (for politeness). If unset, bibtools will
# try to use your git email (from `git config user.email`). For more info see
# https://www.crossref.org/documentation/retrieve-metadata/rest-api/tips-for-using-the-crossref-rest-api/
crossref_email: null
